"use strict";
function fetchWithAuth22(url, method) {
    console.log(method, typeof method);
    return -1;
}
function checkUser(user) {
    user.name.toUpperCase();
    return true;
}
function magic22(superUser) {
    superUser;
}
